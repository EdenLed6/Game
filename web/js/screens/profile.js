// profile.js — stub. Built by parallel agent.
import { el, mount } from "../dom.js";

export function Profile({ host /*, ctx */ }) {
  mount(host, el("div", { class: "screen" },
    el("div", { class: "stub-screen" }, "TODO: built by parallel agent (Profile)")
  ));
}
