// lesson-detail.js — stub. Built by parallel agent.
import { el, mount } from "../dom.js";

export function LessonDetail({ host /*, ctx, params */ }) {
  mount(host, el("div", { class: "screen" },
    el("div", { class: "stub-screen" }, "TODO: built by parallel agent (LessonDetail)")
  ));
}
